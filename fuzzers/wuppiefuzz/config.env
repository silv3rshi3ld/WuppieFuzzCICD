# Default configuration for WuppieFuzz
TARGET_HOST=localhost
TARGET_PORT=3000
TARGET_SCHEMA=http
TARGET_SWAGGER_PATH=/swagger.json
FUZZING_DURATION=1h
REQUESTS_PER_MINUTE=60

# Target-specific overrides can be provided in the workflow
# Example for VAmPI:
# TARGET_PORT=5000
# TARGET_SWAGGER_PATH=/openapi_specs/openapi3.yml

# Example for Juiceshop:
# TARGET_PORT=3000
# TARGET_SWAGGER_PATH=/swagger.json

# Example for DVAPI:
# TARGET_PORT=5000
# TARGET_SWAGGER_PATH=/swagger.json