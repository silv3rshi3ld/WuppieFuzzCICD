apiVersion: 1

datasources:
  - name: WuppieFuzz
    type: frser-sqlite-datasource
    access: proxy
    isDefault: true
    editable: true
    jsonData:
      database: /var/lib/grafana/datasources/report.db
