apiVersion: apps/v1
kind: Deployment
metadata:
  name: vampi-wuppiefuzz
  namespace: fuzzing-system
  labels:
    app: vampi-wuppiefuzz
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vampi-wuppiefuzz
  template:
    metadata:
      labels:
        app: vampi-wuppiefuzz
    spec:
      containers:
      - name: vampi-wuppiefuzz
        image: localhost:5000/vampi-vulnerable-wuppiefuzz:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        env:
        - name: vulnerable
          value: "1"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: vampi-wuppiefuzz
  namespace: fuzzing-system
spec:
  selector:
    app: vampi-wuppiefuzz
  ports:
  - port: 5022
    targetPort: 5000
  type: NodePort
